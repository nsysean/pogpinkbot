
module.exports = {
    name : 'balance',
    
    aliases : ['bal'],
    category : 'economy',
    description : `beg for tickets`,

    /**
     * @param {Client} client
     * @param {Message} message
     * @param {String[]} args
     */

    run : async(client, message, args) => {
     

    } 
}